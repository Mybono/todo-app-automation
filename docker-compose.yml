version: "3.9"

services:
  todo-tests:
    build: .
    container_name: todo-app-tests
    ports:
      - "4723:4723"
    volumes:
      - .:/usr/src/app
      - ./reports:/usr/src/app/reports
    environment:
      NODE_ENV: "test"
